{
  "name": "webgl-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "env": "env",
    "lint": "jshint **.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist/*",
    "watch": "watchify js/app.js -o dist/js/app.js",
    "start:dev": "npm run watch & nodemon --watch js --watch server -e js server/index.js"
  },
  "author": "Juan Andres Andrango <juan@floored.com> (http://www.floored.com/)",
  "license": "ISC",
  "devDependencies": {
    "jshint": "^2.8.0",
    "nodemon": "^1.8.0",
    "watchify": "^3.5.0"
  },
  "dependencies": {
    "ejs": "^2.3.4",
    "express": "^4.13.3",
    "gl-matrix": "^2.3.1"
  }
}
